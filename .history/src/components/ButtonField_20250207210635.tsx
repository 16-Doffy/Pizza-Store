import React, { ReactNode } from 'react';

type Props ={
    loading?:boolean;
    children?: ReactNode;
}
const ButtonField = ({
    loading,
    children,
}: Props) => {
    return (
        <div>
            
        </div>
    );
};

export default ButtonField;