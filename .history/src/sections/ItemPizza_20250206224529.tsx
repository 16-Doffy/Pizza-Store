
import React from 'react';
import { Pizza } from '../models/Pizza.model';
type Props = Pizza;
const ItemPizza = (   {
    title,
    description,
   }:Props ) => {
    return (
        <div>
            this is itemOuzza
        </div>
    );
};

export default ItemPizza;